{
	"name": "flagpole_server",
	"version": "0.1.0",
	"license": "GPL-3.0",
	"scripts": {
		"dev": "tsx src/index.ts",
		"lint": "tsc --noEmit",
		"clean": "rm -rf build/*",
		"start": "node build/index.dist.js",
		"watch": "nodemon --watch 'src/**/*.ts' --exec 'tsx' src/index.ts",
		"build": "yarn clean && yarn lint && esbuild $(esbuild-config)"
	},
	"dependencies": {
		"@hono/node-server": "^0.4.0",
		"got": "^12.6.0",
		"hono": "^3.1.8",
		"nanoid": "^4.0.2"
	},
	"devDependencies": {
		"esbuild-config": "^1.0.1",
		"tsx": "^3.12.2",
		"typescript": "^5.0.4"
	}
}
